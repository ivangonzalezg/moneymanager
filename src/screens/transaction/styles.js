import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  icon: {
    fontSize: 20,
  },
  key: {
    borderRightWidth: 0,
    borderBottomWidth: 0,
  },
});

export default styles;
